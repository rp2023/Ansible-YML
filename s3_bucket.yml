---
- name: Create an S3 bucket
  hosts: localhost
  gather_facts: no
  vars:
    aws_access_key: AKIA4MTWGW4A3SQRUM4F
    aws_secret_key: AKIA4MTWGW4A3SQRUM4F
    region: ap-south-1
    bucket_name: Rahul786
  tasks:
    - name: Create S3 bucket
      amazon.aws.aws_s3:
        aws_access_key: "{{ aws_access_key }}"
        aws_secret_key: "{{ aws_secret_key }}"
        region: "{{ region }}"
        bucket: "{{ bucket_name }}"
        mode: create
...