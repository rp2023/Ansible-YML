---
- hosts: all
  become: yes
  tasks:
    - name: install multiple packages
      yum:
        name: "{{ item }}"
        state: latest
      loop:
        - httpd
        - git
        - maven
...
